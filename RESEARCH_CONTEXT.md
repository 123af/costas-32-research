<!-- COSTAS_RESEARCH_CONTEXT_V1 -->

# Costas-32 Research Context

## 문서 목적과 기준일

이 문서는 차수 32 코스타스 배열의 존재 또는 비존재를 연구하는 에이전트들이 공통으로 사용해야 할 수학적·계산적 배경을 제공한다.

이 문서의 목적은 참고문헌 목록을 대신 만드는 것이 아니라, 현재까지 확립된 연구 내용을 인터넷 검색 없이 바로 사용할 수 있도록 하나의 자립형 문맥으로 정리하는 것이다.

- 기준일: **2026-08-01**
- 연구 대상: 표준적인 2차원 정사각 코스타스 배열
- 기본 표기: **1-based 인덱스**
- 핵심 미해결 문제: **차수 32 코스타스 배열의 존재 여부**
- 완전 열거가 끝난 최대 차수: **29**
- 차수 30과 31: 적어도 하나 이상의 배열이 존재함
- 차수 32와 33: 공개된 주요 연구 기록에서 유효 배열도 비존재 증명도 확인되지 않은 최소 차수들

이 문서보다 최신의 검증된 배열, 완전 열거 결과 또는 증명 인증서가 저장소에 추가되면 그 자료가 우선한다. 단, 단순한 주장·검색 실패·검증되지 않은 후보는 이 문서의 상태를 바꾸지 않는다.

---

# 1. 기본 집합과 순열 표기

양의 정수 \(n\)에 대해

\[
[n] = \{1,2,\ldots,n\}
\]

으로 쓴다.

차수 \(n\)의 배열은 다음 전단사 순열로 표현한다.

\[
\pi:[n]\rightarrow[n].
\]

저장소와 모든 연구 보고서에서는 순열을 다음과 같이 기록한다.

```text
[p1, p2, ..., pn]
```

여기서 \(p_i=\pi(i)\)이며, 이 순열은 다음 \(n\)개의 점을 뜻한다.

\[
(1,p_1),(2,p_2),\ldots,(n,p_n).
\]

첫 번째 좌표는 열 또는 시간 인덱스이고, 두 번째 좌표는 행 또는 값 인덱스다. 이 프로젝트에서는 좌표를 항상

\[
(x,y)=(i,\pi(i))
\]

순서로 기록한다.

## 1.1 1-based 규칙

다음 규칙은 모든 코드, 데이터, 증명 및 보고서에 적용한다.

- 순열의 위치는 \(1,\ldots,n\)이다.
- 순열 값도 \(1,\ldots,n\)이다.
- 길이 \(n\)인 유효 순열은 정수 \(1,\ldots,n\)을 각각 정확히 한 번 포함한다.
- 논문이나 프로그램이 0-based 표기를 사용하면 저장소에 넣기 전에 변환 여부를 명시한다.
- 0-based 순열 \([a_0,\ldots,a_{n-1}]\)을 1-based로 바꿀 때는 위치와 값에 각각 1을 더한다.
- 위치만 또는 값만 1을 더하는 불완전한 변환은 허용하지 않는다.

## 1.2 이진 행렬 표현

순열 \(\pi\)에 대응하는 \(n\times n\) 이진 행렬 \(A\)를

\[
A_{r,c}=
\begin{cases}
1,&r=\pi(c),\\
0,&\text{그 밖의 경우}
\end{cases}
\]

로 정의한다.

각 열과 각 행에는 정확히 하나의 1이 있다. 따라서 코스타스 배열은 특별한 종류의 순열 행렬이다.

---

# 2. 코스타스 성질의 정확한 정의

순열 \(\pi:[n]\to[n]\)가 코스타스 순열이라는 것은 서로 다른 두 점을 잇는 모든 변위벡터가 서로 다르다는 뜻이다.

두 인덱스 \(1\le i<j\le n\)에 대응하는 변위벡터는

\[
(j-i,\ \pi(j)-\pi(i))
\]

이다.

따라서 \(\pi\)가 코스타스 순열일 필요충분조건은 다음과 같다.

\[
(j-i,\pi(j)-\pi(i))
=
(\ell-k,\pi(\ell)-\pi(k))
\]

이면 두 점쌍이 동일해야 한다. 즉 \(i=k\)이고 \(j=\ell\)이어야 한다.

동일한 내용을 고정된 열 간격으로 쓰면 다음과 같다.

모든 \(d\in\{1,\ldots,n-1\}\)에 대해

\[
\Delta_d(i)=\pi(i+d)-\pi(i),
\qquad i=1,\ldots,n-d
\]

를 정의한다. 그러면 \(\pi\)가 코스타스 순열일 필요충분조건은 각 고정된 \(d\)에서

\[
\Delta_d(1),\Delta_d(2),\ldots,\Delta_d(n-d)
\]

가 모두 서로 다른 것이다.

더 직접적으로는 모든 \(d>0\)과 가능한 \(i,j\)에 대해

\[
\pi(i+d)-\pi(i)=\pi(j+d)-\pi(j)
\]

이면 \(i=j\)여야 한다.

## 2.1 중요한 해석 규칙

- 같은 수직 차분이 **서로 다른** \(d\)에서 나타나는 것은 허용된다. 전체 변위벡터의 첫 좌표가 다르기 때문이다.
- 충돌은 동일한 \(d\)에서 동일한 수직 차분이 두 번 나타날 때 발생한다.
- 차분은 정수 차분이며 모듈러 연산이 아니다.
- 부호까지 포함한다. \(+a\)와 \(-a\)는 다른 차분이다.
- 모든 점쌍은 방향을 \(i<j\)로 고정해 검사한다.

## 2.2 기하학적 동치

코스타스 성질은 다음 조건과 동치다.

- 서로 다른 두 점쌍이 같은 변위벡터를 만들지 않는다.
- 네 점이 평행사변형의 꼭짓점을 이루지 않는다.
- 세 점이 한 직선 위에서 같은 간격으로 놓이는 경우도 금지된다. 이는 납작해진 평행사변형에 해당한다.

단순히 모든 기울기가 다르다는 표현만 사용하면 수평 거리 정보가 사라질 수 있으므로, 구현과 증명에서는 변위벡터 또는 고정 간격 차분을 사용한다.

---

# 3. 차분 삼각형

순열 \(\pi=[p_1,\ldots,p_n]\)의 차분 삼각형은 다음 행들로 이루어진다.

\[
T_d(\pi)=
\bigl(
p_{1+d}-p_1,\,
p_{2+d}-p_2,\,
\ldots,\,
p_n-p_{n-d}
\bigr),
\qquad d=1,\ldots,n-1.
\]

\(T_d\)에는 \(n-d\)개의 원소가 있다.

코스타스 성질은 다음과 정확히 동치다.

> 모든 \(d\)에 대해 \(T_d(\pi)\) 안에 중복된 정수가 없다.

따라서 기준 검증기는 다음 순서로 구현할 수 있다.

1. 입력이 \(1,\ldots,n\)의 순열인지 검사한다.
2. \(d=1,\ldots,n-1\)을 순회한다.
3. 각 \(d\)마다 빈 집합을 만든다.
4. 모든 \(i=1,\ldots,n-d\)에 대해 \(p_{i+d}-p_i\)를 계산한다.
5. 같은 \(d\)의 집합에 이미 있으면 즉시 실패한다.
6. 모든 \(d\)가 통과하면 코스타스 순열이다.

완전한 후보 하나의 검증에는 \(n(n-1)/2\)개의 점쌍 또는 차분만 확인하면 되므로 \(O(n^2)\) 시간에 가능하다. 어려운 부분은 후보 하나의 검증이 아니라 유효 후보를 찾거나 전체 탐색 공간을 완전히 소진하는 일이다.

---

# 4. 대칭과 동치류

정사각형의 회전과 반사는 코스타스 성질을 보존한다.

순열 \(\pi\)에서 다음 순열들을 얻을 수 있다.

\[
\pi(i),
\]

\[
n+1-\pi(i),
\]

\[
\pi(n+1-i),
\]

\[
n+1-\pi(n+1-i),
\]

그리고 이 네 순열의 역순열에 해당하는 네 가지 변환이다.

전치는 순열의 역함수

\[
\pi^{-1}
\]

에 해당한다.

이 여덟 변환은 정사각형의 이면체 대칭군 \(D_4\)를 이룬다.

차수 \(n>2\)의 코스타스 배열 하나가 만드는 대칭 궤도는 일반적으로 8개 배열을 포함한다. 대각선 대칭이 있는 경우 궤도 크기는 4일 수 있다.

## 4.1 데이터 처리 규칙

- 회전·반사·전치로 얻은 배열을 서로 독립된 구조적 증거로 세지 않는다.
- 데이터 학습 전에 각 궤도의 정규 대표를 하나 선택한다.
- 기본 정규 대표는 여덟 변환의 순열 중 사전식으로 가장 작은 순열로 한다.
- 논문에서 말하는 “배열 개수”와 “동치류 개수”를 혼동하지 않는다.
- 대칭 변환으로 얻은 배열은 새로운 구성법이나 새로운 해로 주장하지 않는다.
- 후보가 발견되면 원본과 정규 대표를 모두 기록한다.

---

# 5. 완전 열거 결과

차수 \(1\)부터 \(29\)까지는 모든 코스타스 배열이 완전 열거되었다. 다음 \(C(n)\)은 대칭 변환을 포함한 전체 배열 개수다.

| 차수 \(n\) | 정확한 개수 \(C(n)\) |
|---:|---:|
| 1 | 1 |
| 2 | 2 |
| 3 | 4 |
| 4 | 12 |
| 5 | 40 |
| 6 | 116 |
| 7 | 200 |
| 8 | 444 |
| 9 | 760 |
| 10 | 2160 |
| 11 | 4368 |
| 12 | 7852 |
| 13 | 12828 |
| 14 | 17252 |
| 15 | 19612 |
| 16 | 21104 |
| 17 | 18276 |
| 18 | 15096 |
| 19 | 10240 |
| 20 | 6464 |
| 21 | 3536 |
| 22 | 2052 |
| 23 | 872 |
| 24 | 200 |
| 25 | 88 |
| 26 | 56 |
| 27 | 204 |
| 28 | 712 |
| 29 | 164 |

이 표는 완전 열거 결과이므로 \(n\le29\)에서는 알려지지 않은 추가 배열이 존재할 수 없다.

## 5.1 차수 27

차수 27에는 정확히 204개 배열이 있으며 29개의 대칭 동치류로 나뉜다.

동치류 수준의 분류는 다음과 같다.

- 대칭 T4 계열 1개
- W2 계열 6개
- G2 계열 21개
- 기존 생성·파생 계열로 설명되지 않는 비대칭 sporadic 동치류 1개

즉 차수 27에는 sporadic 구조가 남아 있다.

## 5.2 차수 28

차수 28에는 정확히 712개 배열이 있다.

완전 열거에서 발견된 모든 배열은 알려진 Welch·Golomb 계열과 그 당시 인정된 파생 구성으로 설명되며, 차수 5보다 큰 차수 중 처음으로 sporadic 배열이 하나도 없는 차수가 되었다.

완전 열거에는 단일 CPU 환산 약 70년이 들었다.

## 5.3 차수 29

차수 29에는 정확히 164개 배열이 있으며 23개의 대칭 동치류로 나뉜다. 그중 5개 동치류는 대칭 동치류다.

전체 164개의 분해는 다음과 같다.

- G0: 4개 배열, 대칭 동치류 1개
- G2: 64개 배열, 동치류 10개, 그중 대칭 동치류 4개
- G3: 24개 배열, 동치류 3개
- W0: 24개 배열, 동치류 3개
- W2: 32개 배열, 동치류 4개
- RW0: 16개 배열, 동치류 2개

따라서 차수 29에도 sporadic 배열은 없다. 다만 모든 배열이 단순한 기본 W1·G2 공식에서 곧바로 나온다는 뜻은 아니며, 일부는 점 추가나 순환 이동을 포함하는 파생·반경험적 구성이다.

완전 열거의 계산 비용은 다음과 같다.

- 단일 CPU 환산 약 366.55년
- 실제 병렬 실행 기간 약 230일
- 평균 약 568개 프로세서
- 순간 최대 약 2,000개 프로세서

이는 32차 전체를 단순 확장한 무차별 탐색이 현실적으로 매우 어렵다는 강한 계산적 근거다. 그러나 이것은 32차 비존재의 수학적 근거는 아니다.

---

# 6. 현재 존재 상태

## 6.1 완전성이 보장되는 범위

- \(1\le n\le29\): 존재하며 정확한 전체 개수가 알려져 있다.
- \(n\ge30\): 알려진 배열 목록은 일반적으로 불완전하다.
- 차수 30과 31에는 배열이 존재하지만 전체 개수는 알려져 있지 않다.
- 2011년까지 공개된 최소 수치는 차수 30에서 적어도 664개, 차수 31에서 적어도 8개였다. 이 숫자는 하한일 뿐 완전 열거값이 아니다.
- 차수 32와 33에서는 기준일 현재 검증된 배열도 완전한 비존재 증명도 없다.

## 6.2 고차 데이터베이스의 의미

고차의 알려진 배열을 모은 데이터는 1030차까지 존재하지만, 이는 각 차수의 모든 배열을 열거했다는 뜻이 아니다.

고차 데이터는 주로 다음을 포함한다.

- 유한체 구성으로 생성된 배열
- 알려진 파생 구성으로 생성된 배열
- 특정 검색에서 발견된 배열
- 각 배열의 대칭 변환

따라서 \(n>29\)의 데이터에서 어떤 패턴이 보이지 않는다는 사실은 전체 코스타스 배열에서 그 패턴이 불가능하다는 뜻이 아니다.

다음 추론은 금지한다.

> 데이터베이스에 없으므로 존재하지 않는다.

허용되는 표현은 다음과 같다.

> 현재 사용한 불완전 데이터베이스에서는 발견되지 않았다.

## 6.3 비존재 상태

표준 2차원 코스타스 배열에 대해 \(n>1\)인 어떤 차수에서도 전체 비존재가 확정되었다고 취급해서는 안 된다.

특히 차수 32는 다음 두 주장 모두 미확정이다.

\[
C(32)>0
\]

\[
C(32)=0
\]

유효한 길이 32 순열 하나면 \(C(32)>0\)은 해결된다. 반대로 \(C(32)=0\)을 보이려면 일반적인 모순 증명, 완전한 경우분류, 또는 독립 검증 가능한 완전 탐색 인증서가 필요하다.

---

# 7. 구성법의 분류

이 문서에서는 알려진 배열의 기원을 다음과 같이 구분한다.

## 7.1 생성형 구성

차수와 수론적 조건만으로 적용 가능성이 보장되고, 조건이 성립하면 코스타스 성질이 증명되는 무한 계열이다.

대표적으로 W1, W2, G2, G3, G4가 있다.

## 7.2 예측 가능한 파생 구성

이미 생성된 배열에 행·열 삭제 등의 변환을 적용한다. 추가적인 대수 조건이 만족되면 코스타스 성질이 증명된다.

대표적으로 W3와 일부 G4·G5 계열이 있다.

## 7.3 직접 검증이 필요한 파생 구성

점 추가, 순환 이동, 행·열 추가와 같은 변환을 적용하지만, 주어진 매개변수에서 성공하는지는 결과를 직접 검사해야 한다.

대표적으로 G1, G0, W0, RW0, RG1 계열이 있다.

이들은 “알고리즘을 실행하면 항상 코스타스 배열이 나온다”는 구성법이 아니다.

## 7.4 Sporadic 배열

현재 알려진 생성형 또는 파생 구성으로 설명되지 않는 배열을 sporadic 배열이라고 부른다.

“Sporadic”은 절대적인 수학적 성질이 아니라 현재 알려진 구성법에 상대적인 분류다. 새로운 구성법이 발견되면 과거의 sporadic 배열이 구성형으로 재분류될 수 있다.

---

# 8. Welch 계열

## 8.1 W1 구성

\(p\)를 소수, \(\alpha\)를 \(\mathbb F_p^\times\)의 원시근, \(c\in\{0,\ldots,p-2\}\)라 하자.

\[
\pi(i)=\alpha^{i-1+c}\pmod p,
\qquad i=1,\ldots,p-1
\]

로 정의하면 \(\pi\)는 \(1,\ldots,p-1\)의 순열이며 차수 \(p-1\)의 코스타스 배열이다.

핵심 사실은 다음과 같다.

- \(\alpha\)가 원시근이므로 거듭제곱이 모든 비영 원소를 정확히 한 번 순회한다.
- 동일한 열 간격에서 동일한 차분이 생겼다고 가정하면 유한체의 소거법으로 시작 위치가 같음을 유도할 수 있다.
- \(c\)를 바꾸는 것은 지수열의 순환 이동과 관계된다.
- 배열의 전치는 역순열이며 로그형 Welch 배열로 해석할 수 있다.
- \(p>5\)에서는 지수형과 로그형 W1 배열이 서로 겹치지 않으며 W1 배열은 대각선 대칭이 아니다.
- W1은 열 방향의 순환 이동 후에도 W1 배열이 되는 단일 주기성을 가진다.
- 지수형 W1은 두 절반이 상하 반전 관계를 이루는 반반사 대칭을 가진다.

W1이 보장하는 차수는

\[
n=p-1
\]

이며 \(p\)는 소수여야 한다.

## 8.2 W2 구성

W1에서 \(c=0\)이면

\[
\pi(1)=1
\]

이므로 왼쪽 첫 열과 첫 행의 공통 모서리 점을 삭제할 수 있다.

원래 W1 순열을 \(g\)라 하면

\[
f(i)=g(i+1)-1,
\qquad i=1,\ldots,p-2
\]

는 차수 \(p-2\)의 코스타스 배열이다.

이 삭제는 임의의 점 삭제가 아니다. 삭제할 점이 모서리에 있고, 나머지 좌표가 동일한 양만큼 평행 이동되기 때문에 보존 증명이 가능하다.

## 8.3 W3 구성

\(p\)가 소수이고 2가 \(\mathbb F_p^\times\)의 원시근이면 W1 순열

\[
g(i)=2^{i-1}\pmod p
\]

은 처음 두 값이 \(1,2\)가 된다. 해당 두 행과 두 열을 삭제하면 차수 \(p-3\)의 코스타스 배열을 얻는다.

이 구성은 모든 소수에서 가능한 것이 아니다. “2가 원시근”이라는 추가 조건이 필요하다.

## 8.4 W0와 RW0

W0는 W1 배열에 모서리 점 하나를 추가해 차수를 \(p-1\)에서 \(p\)로 늘리는 시도다. 모든 매개변수에서 성공하지 않으며 완성된 배열을 직접 검증해야 한다.

RW0는 W1 배열을 더 큰 좌표계 안에서 순환 이동하여 한 행을 비운 뒤, 새 열의 그 빈 행에 점을 추가하는 방식이다. 역시 성공 여부는 매개변수에 의존하고 직접 검증이 필요하다.

---

# 9. Golomb 계열

\(q=p^m\)을 소수 거듭제곱이라 하고 \(\mathbb F_q\)를 \(q\)개 원소를 가진 유한체라 한다.

## 9.1 G2 구성

\(\alpha,\beta\)를 \(\mathbb F_q^\times\)의 원시원이라 하자.

각 \(i,j\in\{1,\ldots,q-2\}\)에 대해

\[
\alpha^j+\beta^i=1
\]

을 만족하는 유일한 \(j\)를 \(\pi(i)\)로 정한다.

그러면 \(\pi\)는 차수

\[
q-2
\]

의 코스타스 순열이다.

\(\alpha=\beta\)인 특수한 경우는 Lempel형으로 불린다.

G2가 작동하려면 단순히 정수 모듈러 연산을 쓰는 것이 아니라 실제 유한체 \(\mathbb F_q\)와 그 곱셈군의 원시원을 사용해야 한다.

## 9.2 G3 구성

G2의 원시원들이

\[
\alpha+\beta=1
\]

을 만족하면 대응 순열은 모서리 값 \(1\)을 포함한다. 첫 행과 첫 열을 삭제하고 좌표를 1만큼 줄이면 차수

\[
q-3
\]

의 코스타스 배열을 얻는다.

유한체에서 합이 1인 원시원 쌍이 존재한다는 결과 때문에 G3는 생성형 계열로 취급할 수 있다.

## 9.3 G4 구성

\(q=2^m\)이고 \(\alpha,\beta\)가 원시원이며

\[
\alpha+\beta=1
\]

이면 대응 G2 배열에서 처음 두 점을 규칙적으로 삭제하여 차수

\[
q-4
\]

의 코스타스 배열을 얻을 수 있다.

일반적인 \(q\)에 대해 단순히 \(q-4\)차 배열이 항상 나온다는 뜻은 아니다. 표준 G4의 보장 조건에는 \(q=2^m\)이 포함된다.

## 9.4 T4 및 기타 조건부 삭제 계열

G2의 원시원에 다음과 같은 추가 다항식 관계가 성립하면 두 점 또는 세 점을 더 삭제해 \(q-4\), \(q-5\) 차수의 배열을 얻을 수 있다.

대표 조건의 형태는 다음과 같다.

\[
\alpha+\beta=1,
\]

\[
\alpha^2+\beta^{-1}=1,
\]

또는 대칭형의 경우

\[
\alpha+\alpha^2=1.
\]

이 계열은 조건을 만족하는 원시원이 실제로 존재하는지 확인해야 하며, 차수만으로 무조건 적용되는 것은 아니다.

## 9.5 G1과 G0

G1은 차수 \(q-2\)의 G2 배열에 모서리 점 하나를 추가하여 차수 \(q-1\)을 만드는 시도다.

G0는 G2 배열에 서로 반대쪽인 두 모서리 점을 추가하여 차수 \(q\)를 만드는 시도다.

둘 다 모든 원시원 선택에서 성공하지 않는다. 완성된 후보를 직접 검사해야 한다.

## 9.6 Rickard형 Golomb 확장

G2 배열을 한 행·열씩 확대한 뒤 행과 열을 각각 순환 이동하고, 비어 있는 행과 열의 교차점에 점 하나를 추가하는 방식이 있다.

이 방식은 기존 G2를 포함한 파생 계열을 만들 수 있지만, 성공은 이동량과 원시원 선택에 의존한다. 따라서 생성된 후보의 전체 차분을 다시 검사해야 한다.

---

# 10. 차수 32에서 표준 구성들이 막히는 이유

차수 32를 기본 생성형 공식으로 바로 얻으려 하면 다음 매개변수가 필요하다.

| 구성 | 목표 차수 조건 | 필요한 값 | 문제 |
|---|---:|---:|---|
| W1 | \(p-1=32\) | \(p=33\) | 33은 소수가 아님 |
| W2 | \(p-2=32\) | \(p=34\) | 34는 소수가 아님 |
| W3 | \(p-3=32\) | \(p=35\) | 35는 소수가 아님 |
| G2 | \(q-2=32\) | \(q=34\) | 34는 소수 거듭제곱이 아님 |
| G3 | \(q-3=32\) | \(q=35\) | 35는 소수 거듭제곱이 아님 |
| 표준 G4 | \(q-4=32\) | \(q=36\) | 36은 \(2^m\)이 아니며 단일 유한체 차수 조건에도 맞지 않음 |

따라서 32는 기본 W1·W2·W3·G2·G3·표준 G4 계열이 자동으로 채우지 못하는 첫 번째 작은 공백이다.

다만 이것은 차수 32 배열이 없다는 뜻이 아니다. 다음 가능성은 여전히 남아 있다.

- \(q=32\)의 G2 배열에 두 점을 추가하는 G0형 확장
- 더 큰 생성 배열에서 조건부 삭제
- 29·30·31차 배열의 점 추가 또는 행·열 삽입
- 특수 원시원 조건을 이용한 조건부 Golomb 삭제
- Rickard형 순환 이동과 점 추가
- 기존 계열로 설명되지 않는 sporadic 배열
- 새로운 구성법
- 정확 탐색으로 발견되는 배열

## 10.1 단순한 mod 33 Welch 시도가 불가능한 이유

\(\mathbb Z/33\mathbb Z\)는 체가 아니다.

- 33은 소수도 소수 거듭제곱의 체 크기도 아니다.
- 0이 아닌 잉여류 32개가 모두 가역원이 아니다.
- 가역원 개수는

\[
\varphi(33)=20
\]

뿐이다.
- 어떤 \(g\)의 거듭제곱도 32개의 모든 비영 잉여류를 순회할 수 없다.

따라서

\[
g^i\pmod{33}
\]

형태만으로 \(1,\ldots,32\)의 순열을 만드는 Welch식 구성은 불가능하다.

이 방향을 다시 조사하려면 “33을 소수처럼 취급”해서는 안 되고, Welch 증명에 실제로 필요한 순환군·소거법·차분 유일성을 다른 대수 구조에서 어떻게 대체할지를 명시해야 한다.

---

# 11. 배열의 추가·삭제·삽입

## 11.1 안전한 삭제

경계의 첫 행과 첫 열을 함께 삭제하면서 남은 모든 좌표를 동일하게 1씩 줄이는 경우에는 기존 점쌍 사이의 변위가 그대로 보존된다.

W2와 G3의 삭제가 이 유형이다.

## 11.2 내부 삭제의 위험

내부의 행이나 열을 삭제한 뒤 좌표를 압축하면 삭제 위치의 양쪽에 있는 기존 점쌍 사이의 수평 또는 수직 차이가 바뀐다.

따라서 다음만 검사해서는 안 된다.

- 삭제된 점과 다른 점 사이의 충돌

반드시 다음도 검사해야 한다.

- 압축으로 좌표가 바뀐 기존 점쌍끼리의 새로운 충돌

## 11.3 내부 삽입의 위험

내부에 행이나 열을 삽입할 때도 마찬가지다.

새 점을 포함한 변위뿐 아니라, 삽입 위치를 사이에 둔 모든 기존 점쌍의 변위가 달라질 수 있다.

확장 정리는 다음 세 종류를 모두 다뤄야 한다.

1. 새 점과 기존 점 사이의 변위
2. 새 점들 사이의 변위
3. 좌표가 이동한 기존 점쌍 사이의 변위

## 11.4 점 추가의 증명 의무

점 하나를 추가하여 \(n\)차 배열을 \(n+1\)차로 만들었다면 다음을 보여야 한다.

- 결과가 여전히 순열이다.
- 기존 점쌍의 변위 중 새로 같아진 것이 없다.
- 새 점과 모든 기존 점 사이의 변위가 서로 다르다.
- 새 점 관련 변위가 기존 변위와 겹치지 않는다.

프로그램이 전체 후보를 검사하면 특정 후보의 유효성은 확인할 수 있지만, 동일한 변환이 모든 \(n\)이나 모든 입력 배열에서 작동한다는 일반 정리는 별도의 증명이 필요하다.

---

# 12. 확립된 구조적 사실

## 12.1 대칭 궤도 균형

완전한 차수 \(n\) 배열 집합을 모두 쌓으면 각 위치 열에서 순열 값의 평균은

\[
\frac{n+1}{2}
\]

이다.

이는 모든 배열이 회전·반사 궤도 전체와 함께 나타나며, 서로 보완되는 값들의 합이 \(n+1\)이기 때문이다.

이 균형은 완전한 데이터에서 성립하는 대칭 결과다. 불완전한 고차 데이터에서도 대칭 변환을 모두 포함하면 비슷한 균형이 인위적으로 나타날 수 있으므로, 그것만으로 미지 배열의 존재를 예측해서는 안 된다.

## 12.2 빈 사분면 제약

짝수 차수 \(n>6\)의 코스타스 배열은 크기 \(n/2\times n/2\)인 완전히 빈 모서리 사분면을 가질 수 없다.

더 일반적으로 고정된 \(c>0\)에 대해 충분히 큰 \(n\)에서는 크기

\[
\lfloor cn\rfloor\times\lfloor cn\rfloor
\]

인 완전히 빈 모서리 영역을 가질 수 없다.

이 결과는 큰 빈 블록을 허용하는 결합·블록 구성에 강한 제약을 준다.

## 12.3 밀도 감소

전체 \(n!\)개 순열 중 코스타스 배열이 차지하는 비율

\[
\frac{C(n)}{n!}
\]

은 \(n\)에 대해 지수적으로 감소한다는 엄밀한 상한이 알려져 있다.

따라서 큰 \(n\)에서 균일 무작위 순열을 뽑아 완전 배열이 우연히 나오기를 기대하는 방식은 점점 비효율적이다.

이 결과는 코스타스 배열의 절대 개수 \(C(n)\)가 결국 0이라는 뜻이 아니다. 전체 순열에 대한 상대적 밀도가 지수적으로 작아진다는 뜻이다.

## 12.4 W1의 단일 주기성

W1의 열 순환 이동은 다시 W1 배열을 만든다.

현재 알려진 범위에서 W1이 아닌 단일 주기 코스타스 배열은 알려져 있지 않지만, “단일 주기이면 반드시 W1이다”라는 역명제는 일반 정리로 확정되지 않았다.

따라서 단일 주기성을 W1의 충분한 식별 조건으로 사용하지 않는다.

---

# 13. 완전히 기각된 단순 결합법

## 13.1 단순 interlacing

두 작은 배열의 점들을 홀수 좌표와 짝수 좌표에 번갈아 배치하는 단순 interlacing은 비자명한 새 코스타스 배열을 만들지 못한다.

두 입력 배열의 차수가 같거나 1만큼 다르고, 둘 중 하나의 차수가 3 이상이면 공통 변위벡터가 생긴다. interlacing은 그 변위들을 2배로 확대할 뿐이므로 결과에 평행사변형 충돌이 생긴다.

알려진 정리상 이 방식으로 얻는 유일한 코스타스 배열은 자명한 차수 2 사례다.

따라서 다음 방식은 다시 연구하지 않는다.

> 두 코스타스 배열을 홀수·짝수 행과 열에 그대로 교차 배치한다.

새 결합법을 제안하려면 비선형 좌표 변화, 변위 집합 분리, 완충 영역, 선택적 삭제 등 단순 interlacing 정리의 가정을 실제로 벗어나야 한다.

## 13.2 단순 블록 대각 연결

다음 블록 형태는 일반적으로 코스타스 성질을 보존하지 않는다.

\[
\begin{pmatrix}
A_1&0\\
0&A_2
\end{pmatrix}
\]

서로 다른 블록 내부 또는 블록 사이에서 같은 변위가 생길 수 있다.

단순 interlacing과 달리 모든 크기에 대한 완전한 일반 비존재 정리로 취급해서는 안 되지만, 아무 추가 조건 없는 블록 대각 연결은 우선순위가 낮다.

연구 가치가 있으려면 두 블록의 변위 집합이 왜 겹치지 않는지와 블록 사이 변위가 왜 유일한지를 명시해야 한다.

---

# 14. UCM과 UCFM 표현

## 14.1 UCM

차수 \(n\)의 모든 코스타스 순열을 행으로 쌓은 행렬을 \(U_n\)이라 하자.

\[
U_n\in\mathbb N^{C(n)\times n}.
\]

\(n\le29\)에서는 모든 배열이 열거되었으므로 \(U_n\)을 완전하게 만들 수 있다.

\(n>29\)에서 알려진 배열만 쌓은 행렬은 완전 UCM이 아니다.

## 14.2 UCFM

\(F_n\)의 원소 \(f_{v,k}\)를

\[
f_{v,k}
=
\#\{\text{\(U_n\)의 \(k\)번째 열에서 값 \(v\)가 나타나는 행}\}
\]

로 정의한다.

즉 \(F_n\)은 위치 \(k\)에 값 \(v\)가 얼마나 자주 나타나는지 기록하는 \(n\times n\) 빈도 행렬이다.

완전 UCFM은 다음 성질을 가진다.

- 수평·수직·대각 반사 대칭
- 각 행의 합은 \(C(n)\)
- 각 열의 합도 \(C(n)\)
- 대칭 위치의 빈도는 같다

## 14.3 이 표현이 실제로 해결한 것

완전한 \(F_n\)을 이미 알고 있을 때, 각 위치·값의 허용 빈도와 첫 값별 블록 크기를 사용하여 \(U_n\)을 재구성하는 제한된 완전 탐색을 더 빠르게 할 수 있다.

작은 차수의 실험에서는 기존 기준 백트래킹보다 실행 시간이 줄었으며, 보고된 실험 범위에서 큰 차수 쪽 개선 폭은 약 15% 수준으로 안정되는 경향을 보였다.

## 14.4 이 표현이 아직 해결하지 못한 것

이 방법은 미지 차수의 완전 \(F_n\)을 자동으로 알려주지 않는다.

특히 다음은 아직 확정된 결과가 아니다.

- 32차 UCFM의 정확한 예측
- 예측된 UCFM이 실제 완전 배열 집합과 일치한다는 보장
- UCFM만으로 새 차수의 존재 또는 비존재를 증명하는 방법
- AI가 32차 코스타스 배열을 생성했다는 결과

\(n\le29\)의 완전 UCFM과 고차의 불완전 UCFM을 학습 자료로 사용해 고차 UCFM을 예측하는 것은 제안된 연구 방향이다. 완성된 해결책으로 취급하지 않는다.

---

# 15. 정확 탐색에서 확립된 방법

완전 탐색은 순열을 하나씩 전부 만든 뒤 검사하는 방식보다 부분순열을 확장하는 백트래킹이 기본이다.

부분순열에 새 값을 추가할 때 새 점과 기존 점 사이의 차분만 증분 검사하면 이미 생긴 충돌을 즉시 가지치기할 수 있다.

주요 안전한 최적화는 다음과 같다.

- 사용한 순열 값의 비트마스크
- 열 간격별 사용 차분 집합
- 전방 검사
- 대칭 정규화
- 정규 접두어만 확장하는 canonical augmentation
- 첫 값 또는 일부 점을 고정한 독립 shard 분할
- 남은 값과 위치의 완성 가능성 검사
- 부분배열을 이용한 재귀적 열거
- SAT 또는 CP 인코딩

이러한 방법은 탐색 시간을 줄일 수 있지만, 비존재 증명을 위해서는 가지치기 규칙이 해를 제거하지 않는다는 증명과 탐색 공간의 완전한 분할이 필요하다.

## 15.1 완전 탐색의 최소 기록

어떤 영역을 완전히 소진했다고 주장하려면 다음을 기록한다.

- 탐색한 정확한 변수 범위
- 고정한 점 또는 접두어
- 사용한 대칭 제거 규칙
- 각 shard가 서로 겹치지 않는다는 근거
- shard들의 합이 전체 영역을 덮는다는 근거
- 코드 버전
- 실행 명령
- 완료된 노드 또는 shard 수
- 중단·시간 초과된 범위
- 결과 로그와 해시
- 독립 구현의 재검증 결과

시간 초과 또는 일정 시간 동안 해를 찾지 못한 상태는 완전 소진이 아니다.

---

# 16. 우선순위가 낮거나 잘못된 접근

## 16.1 무작위 순열만 반복 생성

큰 차수에서 코스타스 배열의 상대적 밀도는 지수적으로 감소한다. 아무 구조 없이 균일 무작위 순열만 생성하는 방식은 낮은 우선순위다.

휴리스틱 탐색은 거의 코스타스 후보나 국소 패턴을 만드는 용도로는 유효하지만, 실패 자체는 비존재 근거가 아니다.

## 16.2 임의의 순열 합성

코스타스 순열 \(\pi,\sigma\)가 있을 때 합성

\[
\pi\circ\sigma
\]

가 일반적으로 코스타스 순열이 되는 폐쇄성은 없다.

합성을 사용하려면 차분 유일성이 보존되는 별도의 조건을 증명해야 한다.

역순열은 항상 코스타스지만 이는 전치 대칭이며 새로운 구성 원리로 세지 않는다.

## 16.3 상수 더하기와 좌표 재명명

정수 범위 밖으로 나가지 않는 단순 평행 이동은 점 집합의 변위를 보존하지만, 다시 \([n]\)의 순열로 만들기 위한 모듈러 순환은 일반적으로 코스타스 성질을 보존하지 않는다.

W1에서의 특정 순환성은 W1의 대수 구조에서 나오는 특별한 성질이며 모든 코스타스 배열에 적용되지 않는다.

## 16.4 임의의 행·열 삭제

기존 배열에서 점을 삭제한 뒤 남은 행·열을 압축하는 작업은 일반적으로 코스타스 성질을 보존하지 않는다.

경계 삭제와 내부 삭제를 구분해야 한다.

## 16.5 불완전 데이터의 공통 패턴을 필요조건으로 선언

30차 이상 데이터는 완전하지 않다. 알려진 생성 배열에 공통된 특징은 전체 코스타스 배열이 아니라 특정 구성 계열의 특징일 수 있다.

데이터에서 얻은 패턴은 다음 단계로만 사용한다.

1. 정확한 수학 명제로 형식화
2. 작은 차수의 완전 데이터에서 검사
3. 보지 않은 차수에서 검증
4. 최소 반례 탐색
5. 증명 또는 제한 범위의 완전 소진

## 16.6 “거의 해”를 해로 취급

충돌이 하나뿐인 순열도 코스타스 배열이 아니다.

충돌 수가 감소했다는 사실은 휴리스틱 진전일 수 있으나, 최종 후보는 모든 \(n(n-1)/2\) 변위에 대해 중복이 0이어야 한다.

## 16.7 AI 출력 자체를 증거로 사용

언어 모델이 제시한 공식, 보조정리, 배열 또는 비존재 논증은 자동으로 증거가 되지 않는다.

- 배열은 검증기로 검사한다.
- 유한 범위 명제는 정확 탐색으로 반례를 찾는다.
- 일반 명제는 수학적 증명을 검토한다.
- 신규성은 기존 구성과의 동치 여부를 감사한다.

---

# 17. 증거 수준과 상태 용어

모든 연구 결과는 다음 분류를 사용한다.

## 17.1 `definition`

프로젝트가 사용하는 정확한 정의 또는 표기 규칙이다.

## 17.2 `theorem`

가정과 결론이 명시되고, 논리적으로 완결된 증명이 있으며 독립 검토를 통과한 일반 명제다.

## 17.3 `verified_witness`

구체적인 순열 또는 구조가 있고 두 독립 검사 방식으로 조건을 만족함이 확인된 상태다.

차수 32의 `verified_witness` 하나는 존재 문제를 해결한다.

## 17.4 `counterexample`

정확한 일반 주장에 위배되는 구체적 사례가 독립 검증된 상태다.

반례 하나는 해당 일반 주장을 기각하기에 충분하다.

## 17.5 `computationally_complete`

명시된 유한 영역의 모든 경우가 누락 없이 검사되었고 계산 결과를 재현할 수 있는 상태다.

전체 29차 열거가 이 범주에 해당한다.

## 17.6 `exhausted_in_domain`

전체 문제는 아니지만 명시된 제한 영역이 완전히 소진된 상태다.

예:

> 29차 완전 데이터에서 특정 형태의 세 점 경계 확장은 모두 실패한다.

이 상태를 다음 주장으로 확대해서는 안 된다.

> 32차 배열은 존재하지 않는다.

## 17.7 `computational_observation`

완전성이 보장되지 않은 계산에서 관찰된 현상이다.

예:

- 특정 휴리스틱에서 가장 낮은 충돌 수
- 표본 데이터에서의 빈도 패턴
- 일부 매개변수 범위에서 반례가 없었음

## 17.8 `conjecture`

수학적으로 정확한 문장으로 표현되었지만 증명도 완전 반례 탐색도 끝나지 않은 주장이다.

## 17.9 `heuristic`

후보 생성, 탐색 우선순위 또는 실험 방향을 제시하지만 참이라는 보장이 없는 규칙이다.

## 17.10 `open`

존재와 비존재 어느 쪽도 검증되지 않은 문제다.

차수 32 존재 문제의 현재 상태다.

## 17.11 `duplicate`

기존 구성의 매개변수 변경, 대칭 변환, 역순열, 좌표 재명명 또는 이미 알려진 정리와 실질적으로 같은 결과다.

## 17.12 `blocked`

현재 도구·데이터·계산량으로 검증을 진행할 수 없는 상태다.

`blocked`는 거짓이나 비존재를 뜻하지 않는다.

---

# 18. “증명”, “계산 관찰”, “추측”의 엄격한 구분

## 18.1 증명

다음 중 하나를 만족해야 한다.

- 인간이 단계별로 검토 가능한 일반 논증
- 전체 유한 경우를 덮는 완전한 경우분류와 각 경우의 배제
- 원래 명제와 동치인 SAT/CP 인코딩과 독립 검증 가능한 UNSAT 인증서
- 재현 가능한 완전 열거와 독립 구현의 교차 검증

프로그램을 실행해 몇 차수에서 참이었던 것은 일반 증명이 아니다.

## 18.2 계산 관찰

다음과 같은 표현을 사용한다.

> 차수 \(n\le N\)의 완전 데이터에서 성립했다.

> 지정한 매개변수 범위에서는 반례를 발견하지 못했다.

> 탐색 예산 안에서 최저 충돌 수가 \(k\)였다.

관찰한 범위를 반드시 명시한다.

## 18.3 추측

추측은 다음 필드를 가져야 한다.

- 정확한 가정
- 정확한 결론
- 지지 사례
- 실패 사례
- 현재 알려진 최소 검사 범위
- 예상되는 최소 반례
- 증명에 필요한 핵심 단계
- 정확 탐색에 넘길 검증 요청

“패턴이 있어 보인다”는 문장만으로는 정식 추측이 아니다.

---

# 19. 차수 32 연구에서 인정되는 성과

다음 중 하나를 달성하면 실질적인 성과로 기록한다.

## 19.1 존재 해결

길이 32의 순열 하나를 제시하고 다음을 모두 만족한다.

- \(1,\ldots,32\)를 정확히 한 번 포함
- 기준 검증기 통과
- 독립 검증기 통과
- 적대적 검토 통과
- 순열 전체와 검사 결과 보존

## 19.2 비존재 해결

다음 중 하나가 필요하다.

- 모든 32차 후보에 적용되는 모순 증명
- 전체 경우의 완전 분류와 배제
- 전체 대칭류의 완전 탐색
- 검증 가능한 UNSAT 인증서

휴리스틱 실패, 장시간의 무성과, 특정 구성 계열의 실패는 인정하지 않는다.

## 19.3 보조정리

다음 조건을 만족하는 일반 정리다.

- 차수 32 구성 또는 비존재 탐색에 실제로 사용 가능
- 가정과 결론이 정확함
- 증명 또는 완전한 유한 검증이 있음
- 적대적 검토를 통과함

## 19.4 실질적 가설 기각

차수 32 연구에 실제로 사용되던 비자명한 가설에 대해 명시적인 반례를 제시하고 독립 검증한다.

즉석에서 만든 명백히 거짓인 허수아비 가설의 기각은 성과로 세지 않는다.

---

# 20. 연구 에이전트가 기억해야 할 핵심 요약

1. 코스타스 배열은 \(1,\ldots,n\)의 순열이며, 동일한 열 간격에서 수직 차분이 중복되지 않아야 한다.
2. 모든 인덱스와 값은 1-based다.
3. 회전·반사·전치는 새 독립 데이터가 아니라 같은 대칭 궤도다.
4. 전체 배열 개수는 29차까지만 정확히 알려져 있다.
5. 29차에는 정확히 164개가 있고 sporadic 배열은 없다.
6. 30차와 31차 배열은 존재하지만 완전 열거되지 않았다.
7. 32차와 33차는 존재와 비존재가 모두 미해결이다.
8. 고차 데이터베이스는 불완전하므로 부재를 비존재로 해석하지 않는다.
9. W1은 \(p-1\), W2는 \(p-2\), G2는 \(q-2\), G3는 \(q-3\), 표준 G4는 \(2^m-4\) 차수를 만든다.
10. 32는 이 기본 생성형 차수 조건들 사이의 공백이다.
11. mod 33을 소수체처럼 사용하는 Welch 구성은 불가능하다.
12. 임의의 내부 행·열 삭제나 삽입은 기존 점쌍의 변위를 바꾸므로 자동 보존되지 않는다.
13. 단순 interlacing은 비자명한 코스타스 배열을 만들 수 없음이 증명되었다.
14. 무작위·유전·국소 탐색의 실패는 비존재 증거가 아니다.
15. UCM/UCFM은 완전 데이터를 압축하고 탐색을 보조하지만 32차 UCFM이나 배열을 이미 예측한 해결책은 아니다.
16. 후보 하나의 검증은 쉽지만, 비존재의 증명은 전체 공간의 완전성을 요구한다.
17. 모든 새 주장은 증명, 완전 계산, 계산 관찰, 추측, 휴리스틱 중 무엇인지 명시해야 한다.
18. 연구 에이전트는 자신의 주장을 스스로 `verified` 또는 `proved`로 판정하지 않는다.
